# Terraform 이 붙인 태그가 그대로 Ansible 그룹이 된다.
plugin: amazon.aws.aws_ec2
regions: [ap-northeast-2]
filters:
  instance-state-name: running
  "tag:org": cntlp
  "tag:platform": aws
keyed_groups:
  - key: ec2_tags.platform
    prefix: platform
  - key: ec2_tags.role
    prefix: role
