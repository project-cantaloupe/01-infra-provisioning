# 세 영역의 워커를 이 파일 하나로 처리한다.
# 구성은 같고 붙는 노드 라벨만 다르다. 영역별로 나누면 세 파일이
# 시간이 지나면서 서서히 서로 달라진다.
- name: 워커 노드 구성
  hosts: role_worker
  become: true
  roles:
    - common
    - security-hardening
    - vpn-mesh
    - containerd
    - kubeadm-worker
