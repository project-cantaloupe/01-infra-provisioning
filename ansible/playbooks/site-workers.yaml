---
# 플랫폼별 Worker도 같은 가입 절차를 사용하고 라벨 값만 인벤토리에서 받는다.
- name: 워커 노드 구성
  hosts: role_worker
  become: true
  gather_facts: true
  vars_files:
    - ../vars/cluster.yml
  roles:
    - role: vpn-mesh
      vars:
        tailscale_manage_packages: false
    - kubeadm-worker
